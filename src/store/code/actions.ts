import { ActionTree } from "vuex";
import { ICodeState } from "@/store/code/code";
import { IRootState } from "@/store/root";

const actions: ActionTree<ICodeState, IRootState> = {

};

export default actions;
