import { ActionTree } from "vuex";
import { ISocketState } from "@/store/socket/socket";
import { IRootState } from "@/store/root";

const actions: ActionTree<ISocketState, IRootState> = {};

export default actions;
