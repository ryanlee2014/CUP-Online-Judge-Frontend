import state from "./userInfo/state";
import mutations from "./userInfo/mutations";
import actions from "./userInfo/actions";
import getters from "./userInfo/getters";

const userInfo = {
    state,
    mutations,
    actions,
    getters
};

export default userInfo;
