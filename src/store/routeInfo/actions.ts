import { ActionTree } from "vuex";
import { IRouterInfo } from "@/store/routeInfo/routerInfo";
import { IRootState } from "@/store/root";

const actions: ActionTree<IRouterInfo, IRootState> = {

};

export default actions;
